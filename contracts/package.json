{
  "name": "@flowpay/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "FlowPay Solidity contracts — Foundry project. Scripts thinly wrap forge so pnpm -r test reaches forge test.",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vv",
    "coverage": "forge coverage",
    "snapshot": "forge snapshot"
  }
}
